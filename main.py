a = float(input('Enter a number (enter lowest number first):'))
b = float(input('Enter another number (enter higher number second):'))
# Program to generate a random number between a and b

# importing the random module
import random

print(random.randint(a,b))